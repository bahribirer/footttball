# Üretim yığını (AWS EC2).
#   docker compose up -d --build
#
# Veritabanı ve logo önbelleği imaja gömülmez; sunucudaki dizinlerden bağlanır.

services:
  backend:
    build:
      context: ./backend
    image: tikitakatoe-backend:latest
    restart: unless-stopped
    expose:
      - "8000"
    environment:
      ENVIRONMENT: production
      DEBUG: "false"
      DB_PATH: /app/data/tikitakapi.db
      LOGO_DIR: /app/static/logos
    volumes:
      - ./backend/data:/app/data:ro
      - logo_cache:/app/static/logos
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request,sys; sys.exit(0 if urllib.request.urlopen('http://127.0.0.1:8000/ping', timeout=4).status==200 else 1)"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 20s
    networks:
      - edge
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"

  nginx:
    image: nginx:1.27-alpine
    restart: unless-stopped
    depends_on:
      backend:
        condition: service_healthy
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./infra/nginx/nginx.conf:/etc/nginx/nginx.conf:ro
      - ./infra/nginx/conf.d:/etc/nginx/conf.d:ro
      - ./infra/certbot/conf:/etc/letsencrypt:ro
      - ./infra/certbot/www:/var/www/certbot
    networks:
      - edge
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"

  # Sertifika yenileme: 12 saatte bir dener, nginx'i yeniden yükler.
  certbot:
    image: certbot/certbot:latest
    restart: unless-stopped
    volumes:
      - ./infra/certbot/conf:/etc/letsencrypt
      - ./infra/certbot/www:/var/www/certbot
    entrypoint: >
      sh -c "trap exit TERM;
             while :; do certbot renew --webroot -w /var/www/certbot --quiet;
             sleep 12h & wait $${!}; done"

volumes:
  logo_cache:

networks:
  edge:
    driver: bridge
